import classes from "./avaDescription.module.css";

const Description = (props) => {
    return (
        <div className = {classes.description}>
            pic + description
        </div>
    )
}

export default Description;